// type User = { //using type we can also specify the type for an identifier as well as an object
//   id: string;
//   name: string;
//   avatar: string;
// };

//or use an interface

export interface User { //interface is just limited to objects
  id: string;
  name: string;
  avatar: string;
}
